export const PLUGIN_ID = 'excel-export';

// RBAC action uid (full id: plugin::excel-export.export).
export const EXPORT_ACTION_UID = 'export';
export const EXPORT_ACTION_ID = `plugin::${PLUGIN_ID}.${EXPORT_ACTION_UID}`;
